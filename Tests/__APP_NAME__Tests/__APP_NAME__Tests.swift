import XCTest
@testable import __APP_NAME__

/// Baseline unit test so the `test` lane has a target to compile and run.
/// Replace/extend with real tests as features land.
final class __APP_NAME__Tests: XCTestCase {
    func testSmoke() throws {
        XCTAssertTrue(true, "test target builds and runs")
    }
}
